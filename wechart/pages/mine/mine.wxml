<view class="setimg">
  <text>我女朋友</text>
  <view class="imgs">
    <image src="/image/130.jpg" mode="aspectFit"></image>
  </view>
  <view class="setmap">
    <map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="{{scale}}" controls="{{controls}}"
      bindcontroltap="controltap" markers="{{markers}}" bindmarkertap="markertap" polyline="{{polyline}}"
      bindregionchange="regionchange" show-location style="width: 100%; height: 300px;margin-bottom: 60px;">
    </map>
  </view>
</view>